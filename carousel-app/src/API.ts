// import axios, { AxiosResponse } from "axios";

// const baseUrl: string = "http://localhost:3600/api";

// export const getImages = async () => {
//   try {
//     const images = await axios.get(baseUrl + "/carousel");
//     return images.data.data;
//   } catch (error) {
//     throw new Error(error);
//   }
// };
